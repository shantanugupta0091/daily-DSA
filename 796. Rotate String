#leetcode




bool rotateString(string s, string goal) {
        int n1 = s.size();
        int n2 = goal.size();
        if(n1!=n2) return false;

        string tempstr=s+s;
        
        
        return tempstr.find(goal) != string::npos;
    }
};
